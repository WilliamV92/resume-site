language: node_js
node_js:
  - "10.4"
branches:
  only:
    - master
before_script:
  - npm install -g @angular/cli
script:
  - ng build --prod --aot
install:
  - npm install -g firebase-tools
deploy:
  skip_cleanup: true
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
  project: "resume-site-46fc4"